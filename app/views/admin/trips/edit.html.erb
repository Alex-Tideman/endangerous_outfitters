<article>
  <h2>Update a Trip</h2>
  <%= form_for([:admin,@trip], html: {multipart: true}) do |f| %>
      <%= f.label :name, "Trip Name:" %>
      <%= f.text_field :name %>
      <%= f.label :description, "Description:" %>
      <%= f.text_area :description %>
      <%= f.label :trip_cost, "Price:" %>
      <%= f.number_field :trip_cost %>
      <%= f.label :lat, "Trip Latitude:" %>
      <%= f.text_field :lat %>
      <%= f.label :long, "Trip Longitude:" %>
      <%= f.text_field :long %>
      <% if @trip.image.exists? %>
          <%= image_tag @trip.image.url %><br/>
      <% end %>
      <%= f.label :image, "Pick a New Image" %>
      <%= f.file_field :image %>
      <%= f.submit "Update Trip" %>
  <% end %>
</article>